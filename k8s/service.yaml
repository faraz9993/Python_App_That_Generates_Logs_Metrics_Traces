apiVersion: v1
kind: Service
metadata:
  name: my-observability-service
  labels:
    app: my-observability-service
spec:
  type: LoadBalancer
  selector:
    app: my-observability-service
  ports:
    - name: http
      port: 80
      targetPort: http
